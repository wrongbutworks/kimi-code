---
"@moonshot-ai/kimi-code-sdk": minor
---

Add `session.promptWithSkills(input, skills)` to submit one or more skill activations together with a prompt as a single grouped submission (v2 engine only; rejects on the v1 engine).
